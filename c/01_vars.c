#include <stdio.h>

int main () {

	/* variable is a container/storage area;
	it must have a name which called identifier;	
	naming identifires have same rules at all languages */

	/* initialize, declare, use. */
	int age = 22; printf("%d",age);

	/* but after all its a variable, it can change anytime;
	if you want to declare a constant value */
	const float pi = 3.14;

	/* there is some escapes like
	\n for new line
	\t for new tab
	and others */

return 0;}
